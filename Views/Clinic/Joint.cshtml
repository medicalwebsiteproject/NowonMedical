<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="~/css/subpage.css" asp-append-version="true" />
    </head>
    <body>
        <div class="select_box">
            <div class="inner align-text center">
                <ul>
                    <li><span class="select show">퇴행성관절염</span> </li>
                    <li><span class="select">방아쇠수지</span> </li>
                    <li><span class="select">무릎관절통증</span> </li>
                    <li><span class="select">좌골신경통</span> </li>
                    <li><span class="select">어깨통증</span> </li>
                    <li><span class="select">발목염좌</span> </li>
                </ul>
            </div>
        </div>
        <div class="img_box">
            <img id="contents_image" src="~/assets/images/joint_contents_1.png" />
        </div>

    <script>
        const select_option = document.querySelectorAll("div.select_box div.inner ul li span.select");
        const image = document.getElementById("contents_image");

        const test = document.querySelector("div.select_box div.inner ul li span.select.show");

        const source = [
            "../assets/images/joint_contents_1.png",
            "../assets/images/joint_contents_2.png",
            "../assets/images/joint_contents_3.png",
            "../assets/images/joint_contents_4.png",
            "../assets/images/joint_contents_5.png",
            "../assets/images/joint_contents_6.png"
        ];

        const showImage = (idx) => {
            image.src = source[idx];
        };

        select_option.forEach((selection, idx) => {
            const selectClass = selection.classList;
            selection.addEventListener("click", () => {
                console.log(test);
                const show = document.querySelector("div.select_box div.inner ul li span.select.show");
                if (selectClass.contains("show")) {
                    return;
                }
                show.classList.remove("show");

                selectClass.add("show");

                showImage(idx);
            });
        });
    </script>
    </body>
</html>